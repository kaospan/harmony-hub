# Client-side (Vite) env vars
# These are safe for the browser; provide non-secret values only.
VITE_SUPABASE_URL="https://<your-project>.supabase.co"
VITE_SUPABASE_PUBLISHABLE_KEY="<your-supabase-anon-key>"
# Optional: enable YouTube Data API search connector
VITE_YOUTUBE_API_KEY="<optional-youtube-api-key>"

# Server-side / deployment secrets (do NOT expose in client bundles)
# Set these in your hosting/platform secrets, not in a committed .env.
SPOTIFY_CLIENT_ID="<set-in-hosting-secrets>"
SPOTIFY_CLIENT_SECRET="<set-in-hosting-secrets>"
SPOTIFY_REDIRECT_URI="https://<your-project>.functions.supabase.co/spotify-oauth-callback"
APP_REDIRECT_SUCCESS="https://your-app.example.com/connections?spotify=connected"
APP_REDIRECT_ERROR="https://your-app.example.com/connections?spotify=error"
